# Plugin Core
A library that provides interfaces for creating Custom Devices.

# ## Built Availability
This library is built into a separate package from most of the other components in this repositry. As such, the post-build action copies the built output to a Plugin directory.